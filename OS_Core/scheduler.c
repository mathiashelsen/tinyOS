#include "scheduler.h"

const struct scheduler mainScheduler;
struct scheduler * const sPtr = &mainScheduler;

int initScheduler	( )
{
	/*
	sPtr->numTasks		= 0;
	sPtr->activeTask	= 0;
	sPtr->
	*/
	return 0;
}
